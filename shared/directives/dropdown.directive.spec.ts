import {ComboboxDirective} from './dropdown.directive';

describe('ComboboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboboxDirective();
    expect(directive).toBeTruthy();
  });
});
